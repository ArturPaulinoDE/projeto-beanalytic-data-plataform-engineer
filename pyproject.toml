[project]
name = "selic-platform-pipeline"
version = "0.1.0"
description = "Pipeline de Dados Taxa SELIC com Airflow, Great Expectations e UV"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "pyarrow>=12.0.0",
    "requests>=2.31.0",
    "great-expectations>=0.18.0",
]

